# Optional SharePoint upload helper — replace with your working version if you want automatic uploads.
# Expected API if you wire it up in export_panel.py:
# def upload_export_to_sharepoint(path_in_site: str, file_bytes: bytes, link_scope: str="organization") -> str:
#     return "https://..."  # a URL to the uploaded file
